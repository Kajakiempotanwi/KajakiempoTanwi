import * as React from "react";
import Layout from "../components/layout";
import Seo from "../components/seo";

const IndexPage = () => {
    return (
        <Layout>
            
        </Layout>
    );
};

export const Head = () => <Seo title="Kajaki" />;

export default IndexPage;
